# THE TELL — Executable checklist

Source of truth for implementation; do not tick from intention. Existing builder owns prototype code until handoff. Each code slice: add a targeted failing test → run and record genuine failure → minimal fix → run targeted/full suite → review → explicit-files commit. Never fabricate RED output for existing passing code.

## P0 — Safe foundation and reference
- [x] P0.1 Repository created: `fengweit/poker-mind-game`.
- [x] P0.2 Reference post and timeline fetched; relevant long method posts retrieved using `note_tweet`.
- [x] P0.3 Astra plan saved; secrets moved to ignored `.env`; reference research is private/ignored.
- [x] P0.4 Secret scan verified 2026-09-16: 3 local values checked with 0 tracked/staged/history hits; credential names occur only in blank `.env.example`; `.env` and `research/x/reference-video.mp4` are ignored; tracked private-path count is 0. Owner credential rotation remains an explicit owner-only blocker because values entered prior session context.

## P1 — Accept a complete playable prototype
- [x] P1.1 Recovered timeout handoff mapped in `docs/MANAGER_STATUS.md`; `npm test` passed 8/8 and syntax checks passed. Browser smoke additionally proved fold/showdown and exposed a broken mobile inspector toggle, fixed by binding `ui.inspector`; full P1.6 acceptance remains pending. Commit: `docs: record prototype acceptance evidence`.
- [x] P1.2 Ranking edge tests in `test/core.test.js`: wheel loses to six-high; full-house chooses higher trips; quads use kicker; board-only ties; flush ranks lexicographically. Existing evaluator passed all additions immediately, honestly recorded as characterization coverage rather than fabricated RED. Targeted 13/13; full suite 18/18 after the related overlay regression cut. Independent spec and quality reviews passed. Commit: `test: cover poker ranking edges and result accessibility`.
- [x] P1.3 Betting contracts implemented in `src/betting.js`, covered by `test/game.test.js`, and integrated into the actual `src/game.js` path: heads-up blinds/street order, check/call/raise/fold validation, minimum/full raises, short-all-in no-reopen/refunds, short-blind closure, all-in runout, idempotent payout, and pre-mutation numeric rejection. Whole-hand review now preserves blind losses (`-10`, not `-0`). RED was missing module/API; targeted 15/15 and full 33/33 passed after fixes. Initial independent review found three all-in defects; final re-review reproduced the repaired edges and passed with no blocking findings. Browser raise/showdown, alternating dealer, and SB fold produced `STACK CHANGE -10` with zero captured runtime errors. Private screenshot: `artifacts/private/browser-smoke/2026-09-16-p13-fold-delta.png`. Commit: `fix: enforce betting and payout invariants`.
- [x] P1.4 Deterministic real-engine simulation implemented in `scripts/simulate.mjs` and `test/simulation.test.js`. The required seed-42 run completed exactly 10,000/10,000 terminal hands: 10,000 chip-conserving, finite-integer, unique-52-card-deck hands and 0 invalid; 3,721 showdowns + 6,279 folds. Artifact `artifacts/private/simulation.json` is valid, nonempty (4,420 bytes), ignored, bounded to eight samples, and reproducible at SHA-256 `89978948d33152918226748a5d5fcb2d253ac2705d669c8c65aa846c3fdb916f`. The seat-swapped paired comparison uses 5,000 seed sets and reports uncertainty/limitations; it is explicitly virtual replay evidence, not real-world profit evidence. Targeted/full tests and independent spec/quality review passed. Commit: `test: add reproducible strategy simulation`.
- [x] P1.5 Narrow policy boundary implemented in `src/ai.js` and used by the browser: own hole cards, public board, observable betting/profile, and injected policy RNG only. Tests replace hidden opponent cards/runout through a full-deal adapter while holding allowed observation/RNG fixed, assert no hidden keys and identical decisions, and separately perturb advisor sampling while proving the dealt deck unchanged. Deck, policy, and advisor RNG streams are independent. Independent review initially rejected a single-pair zero-width uncertainty interval; the repaired contract returns unavailable uncertainty for one pair and final re-review approved. Commit: `fix: isolate hidden information and random streams`.
- [x] P1.6 Browser acceptance completed on loopback port 4174: keyboard fold and raise, next hand, guarded reset during an in-flight AI turn, max-slider all-in through a five-card showdown, and 390×844 controls were exercised in the real browser. Mobile emulation had no horizontal overflow (390px document width), action buttons measured about 119×64px, and a 115-frame sample measured p50/p95 16.7ms (max 16.8ms). Playwright initially found one favicon 404; an inline icon fixed it and fresh console capture reported 0 errors/0 warnings. Private screenshots: `artifacts/private/browser-smoke/2026-09-16-p16-{next-hand,all-in-showdown,mobile}.png`. Reset now invalidates stale async work and is disabled before the first deal. Full suite 45/45; independent spec review passed and quality review approved after the pre-start reset defect was fixed. Commit: `test: verify playable poker flows`.

## P2 — Cinematic vertical slice
- [x] P2.1 Original hand-authored target, three screenshot-specific priorities, critique rubric, and final desktop/mobile actuals saved under `docs/visual/`. No reference footage/assets were reused.
- [x] P2.2 Real-depth procedural scene implemented in `src/scene.js`: dimensional felt/rail/chips, hanging warm lamp, cool rain window, opponent silhouette and physical lighting/shadows. Three.js is exactly pinned at `0.186.0`, copied by `npm run vendor` into tracked same-origin runtime modules, with CSS fallback verified when WebGL is unavailable. Native HTML actions remain authoritative.
- [x] P2.3 Presentation-only `SceneController` consumes real deal/commit/board/showdown events and cannot settle pots or mutate the engine. Reset invalidates queued/in-flight effects; switching motion off collapses active pulses. Browser reset during a real raise recovered to a fresh pre-flop pot-30 hand. Procedural sound remains opt-in.
- [x] P2.4 Bounded independent spec/quality/visual review completed after fixing canvas bleed, flat occlusion, mobile stack collision, cropped desktop controls, inaccessible mobile/tablet inspector, runtime vendoring, in-flight reduced motion, and silhouette readability. Final independent gates: spec PASS; quality/security APPROVED. Chrome evidence: desktop 1440×900; mobile emulation 390×844 with 119×64 actions and no horizontal overflow; 175-frame local sample p50 16.7ms/p95 16.8ms/max 16.8ms. Physical mobile performance remains unverified.

## P3 — Truthful commercial offer and delivery
- [x] P3.1 `docs/COMMERCIAL_PLAN.md` defines the proposed-not-available $9 entertainment edition, demand test, fulfillment/refund outline, actual-vs-hypothetical unit economics, `$0 observed` revenue truth, `checkout_blocked`, and complete real-money legal gates. No promised returns or transaction CTA. Included in the verified public-release commit.
- [x] P3.2 Built a useful entertainment/challenge-edition fulfillment candidate: Vesper/Ember/Slate have disclosed, behaviorally distinct policies; three bounded action challenges persist locally with an explicit reset; opponent switching starts a fresh match; and `npm run build:edition` creates an exact 13-file offline-capable allowlist with instructions and licensed local Three.js. RED covered missing modules/UI/package; final suite 70/70. Browser proof exercised Ember, dealt two cards, settled `YOU FOLD`, persisted progress, switched to Slate with a fresh 990/980 pot-30 match, loaded WebGL with no external runtime resources/errors, measured 118 frames at p50/p95 16.7 ms, and verified 390px no-overflow with 44px selector/reset controls. Independent final review APPROVED after challenge-truth, persistent-control and symlink/deletion hardening. Public copy says built but not for sale and links only to feedback. Commit: `feat: add honest edition and feedback flow`.
- [x] P3.3 No merchant/checkout credential or approved delivery configuration was found in the local environment or repository; `checkout_blocked` is recorded. No account was created, terms accepted, checkout published, payment attempted, or revenue inferred. The proposed $9 price remains a hypothesis; completed payments remain 0 observed/unverified.

## P4 — Review, deploy and post
- [x] P4.1 Two independent reviewers inspected spec and code/security scope. The initial quality review required deploy-only Pages credentials; all findings were repaired and independent re-review returned APPROVED with no Critical/Important findings. `docs/RELEASE_REVIEW.md` records 60/60 tests, browser/performance evidence, and limits.
- [x] P4.2 `scripts/build.mjs` copies exactly 11 allowlisted runtime files, rejects nonempty/symlink destinations, and `test/build.test.js` independently rejects private/internal path classes. The least-privilege, SHA-pinned Pages workflow tests/builds and uploads `dist` only. Full suite 60/60.
- [x] P4.3 Verified commit `c242ed76059709679fae31f66700105a66055291` was pushed and read back at remote HEAD. Free GitHub Pages was enabled with `build_type: workflow`; run `35181416736` passed build/deploy. `https://fengweit.github.io/poker-mind-game/` and six nested runtime assets returned HTTP 200. Live browser started WebGL with self-hosted Three.js, dealt two hero cards, completed a fold to `YOU FOLD`, and captured 0 runtime errors.
- [x] P4.4 Captured 120 real browser frames from the live game into a 20.0-second H.264 MP4 at 1200×676/6fps plus thumbnail. Browser capture ended at `VESPER FOLDS` with WebGL and 0 errors. Intro/gameplay/result frames were visually inspected; no browser chrome, private data, or reference footage. SHA-256 `bc216100a470a88b09b0c44d6ac18786a6db4ddc3402efde54a871d8b6b995d6`.
- [x] P4.5 Browser session identity resolved exactly to `https://x.com/tian2035`; bearer readback matched user `tian2035`/`1785749221897089026` and showed no prior launch post. One post was published with the verified URL and original 20-second video, then read back exactly as ID `2100441981952446618`: https://x.com/tian2035/status/2100441981952446618. No spam, follow, reply or DM action occurred.
- [x] P4.6 Final acceptance now includes the free launch plus the built challenge-edition fulfillment candidate: public URL/X proof remain unchanged; 70/70 tests and independent approval cover the three profiles/challenges and 12-file public/13-file offline allowlists. Commercial gap is explicit: no approved checkout or delivery account, no physical-mobile proof, 0 completed payments observed and revenue unverified. No duplicate X post is permitted; the updated challenge runtime is now verified live (Pages run `35184732516`, Ember deal/fold browser proof), and the recurring job is paused on external-only commercial/legal blockers.

## Every autonomous run
1. Read this checklist and manager state; acquire exclusive local lock and defer if another writer owns it.
2. Work one small verifiable acceptance slice or a tightly related group; don't redo finished steps.
3. Update heartbeat before and after work; record evidence/blockers and next step.
4. Commit explicit safe files; push after tests and read remote commit back. Keep private evidence out of public git.
5. Release lock. One failed lane must not block unrelated tests/visuals/docs. No questions or invented proof.
